import styled from "styled-components"

const StyledButton = styled.button`
    background-color: red;
`

function Button() {
    return(
        <StyledButton>Baixar currículo</StyledButton>
    )
}

export default Button

